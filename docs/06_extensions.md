# Extensions (Optional)

These are optional upgrades once the core structure is understood.

Ideas:
- Add parameterization from CSV
- Add basic correlation (web_reg_save_param)
- Add a Markov/state-machine workload model
- Add environment switching (BASE_URL per env)
- Add richer error handling and abort rules
