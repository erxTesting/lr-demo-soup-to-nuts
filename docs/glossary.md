# Glossary

Vuser:
- A virtual user in LoadRunner.

Iteration:
- One pass through Action() (may contain multiple steps).

Transaction:
- A timed block of work (lr_start_transaction / lr_end_transaction).

Workload model:
- The logic that decides what actions happen and how often.
